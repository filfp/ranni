node_modules/
dist/

# runtime files
.agent-queue.json
tools/ranni/runs/

# generated by init (when testing locally) — anchored to repo root so the
# templates/ source-of-truth copies stay tracked
/.agents.yaml
/.mcp.json
/.claude/
