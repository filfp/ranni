{
  "name": "ranni",
  "version": "0.1.0",
  "description": "Provider-agnostic multi-agent orchestration MCP server",
  "type": "module",
  "files": [
    "src",
    "templates"
  ],
  "bin": {
    "ranni": "./src/index.ts"
  },
  "scripts": {
    "start": "bun run src/index.ts",
    "init": "bun run src/init.ts",
    "build": "bun build --compile src/index.ts --outfile dist/ranni",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0",
    "yaml": "^2.4.0",
    "zod": "^3.22.0"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.4.0"
  }
}
